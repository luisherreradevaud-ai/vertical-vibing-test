# Terraform Backend Configuration
# Copy this file to backend.tf and customize for your environment

# Option 1: S3 Backend (recommended for production)
terraform {
  backend "s3" {
    bucket         = "your-terraform-state-bucket"
    key            = "email/terraform.tfstate"
    region         = "us-east-1"
    encrypt        = true
    dynamodb_table = "terraform-state-locks"

    # Optional: Use workspace-based state files
    # workspace_key_prefix = "environments"
  }
}

# Option 2: Local Backend (for development/testing)
# terraform {
#   backend "local" {
#     path = "terraform.tfstate"
#   }
# }

# Option 3: Terraform Cloud
# terraform {
#   backend "remote" {
#     organization = "your-org"
#
#     workspaces {
#       name = "email-infrastructure"
#     }
#   }
# }
